version: "3.7"
services:
  backend:
    build:
      args:
        ENV: development
    command: ["yarn", "--cwd", "services/backend", "start"]
  frontend:
    build:
      args:
        ENV: development
